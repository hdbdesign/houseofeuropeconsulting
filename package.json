{
  "name": "house-of-europe-consulting",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build",
    "dev": "cd client && npm run dev",
    "preview": "cd client && npm run preview"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.3"
  },
  "dependencies": {
    "@radix-ui/react-toast": "^1.2.11",
    "class-variance-authority": "^0.7.1",
    "gsap": "^3.13.0",
    "swiper": "^11.2.6",
    "tailwind-merge": "^3.2.0"
  }
}
